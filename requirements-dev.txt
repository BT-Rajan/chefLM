-r requirements.txt

# Grammar correction (grammar.py) — optional, fails soft if unavailable.
# Downloads a JRE + ~200MB LanguageTool bundle on first use.
language-tool-python>=2.7.0

# Testing
pytest>=7.0.0
